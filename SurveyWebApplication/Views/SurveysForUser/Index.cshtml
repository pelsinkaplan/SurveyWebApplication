@model IEnumerable<SurveyWebApplication.Models.Survey>

@{
    ViewData["Title"] = "Katıldığım Anketler";
}

<h1>Katıldığım Anketler</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Header)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Details)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Deadline)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Header)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Details)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Deadline)
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id">İncele</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    <a asp-action="SurveysPage">Anket Sayfasına Geri Dön</a>
</div>